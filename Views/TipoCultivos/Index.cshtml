@model IEnumerable<Prototipos_Huertos_Autosustentables.Models.TipoCultivo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreTipoCultivos)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreTipoCultivos)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoCultivos">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoCultivos">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoCultivos">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
